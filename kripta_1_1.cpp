#include <iostream>
#include <vector>
#include <cstdint>
#include <stdexcept>
#include <functional>
#include <cassert>
enum class order{lsb,msb};
enum class indexbase{zero,one};
class permutation {
public:
	static std::vector<uint8_t> permute(
		const std::vector<uint8_t>& input
	)
};